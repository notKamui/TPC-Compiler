struct non {
    int x, y;
};

struct non test;

void coucou(void) {
    struct non allo;
    allo.y = 100;
    test = allo;
}

int main(void) {
    struct non a, b;
    b.y = 50;
    b.x = 5;
    a = b;
    a.y = a.y + 2;

    coucou();

    return test.y;
}
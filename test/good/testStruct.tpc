int a;
char un;
char stp;
int fonctionne;

struct bar {
    int x;
    int y;
};

struct bar t;

struct bar foo(struct bar c, int arg1, char arg2) {
    return c;
}

int main(void) {
    a = 1;
    un = 'a';
    t = foo(t, a, un);

    print(a);
    print(un);

    return 0;
}